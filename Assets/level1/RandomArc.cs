using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RandomArc : MonoBehaviour
{

    public GameObject child;
    void Start()
    {
   // child.transform.localPosition = new Vector3(Random.Range(-2, -1), Random.Range((float)0.6, (float)2.1), child.transform.localPosition.z);
   // child.transform.localPosition = new Vector3((float)2, (float)-1, 0);
    }

}
